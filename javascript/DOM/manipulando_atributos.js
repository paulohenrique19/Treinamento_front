const input = document.querySelector("input")

// setAttribute(), atualiza/adiciona um novo atributo
// ex: Aqui estamos desativando o input:
// input.setAttribute("disabled", true)

// ex: Troca o tipo do formulário para envio de arquivo
// input.setAttribute("type", "file")


// Remover um atributo
// input.removeAttribute("id")